name: UI Tests & Allure Report

on:
  workflow_dispatch: # Ручной запуск

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x' # Рекомендую 8.0 или 9.0 как наиболее стабильные сейчас

      - name: Restore dependencies
        run: dotnet restore

      - name: Run Tests
        # Проверь, что в allureConfig.json путь указан именно allure-results
        run: dotnet test --no-restore --verbosity normal
        continue-on-error: true

      - name: Get Allure history
        uses: actions/checkout@v3
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages

      - name: Generate Allure Report
        uses: simple-elf/allure-report-action@master
        if: always()
        with:
          allure_results: **/allure-results
          allure_history: allure-history
          keep_reports: 20

      - name: Deploy Allure Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        if: always()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          # ВАЖНО: Деплоим именно сгенерированный отчет из папки allure-report
          publish_dir: ./allure-report
          force_orphan: true # Создаст ветку gh-pages, если она отсутствует